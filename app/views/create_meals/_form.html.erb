<%= form_for(@create_meal) do |f| %>
  <% if @create_meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@create_meal.errors.count, "error") %> prohibited this create_meal from being saved:</h2>

      <ul>
      <% @create_meal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label "Photo" %><br>
    <%= f.file_field :avatar %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
